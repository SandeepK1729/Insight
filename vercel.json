{
  "version": 2,
  "builds": [
    { "src": "frontend/package.json", "use": "@vercel/static-build", "config": { "out": "frontend/build" } },
    { "src": "backend/Insight/wsgi.py", "use": "@vercel/python", "config": { "maxLambdaSize": "200mb" } }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "backend/Insight/wsgi.py" },
    { "src": "/(.*)", "dest": "frontend/build/" }
  ],
  "env": {
    "DJANGO_SETTINGS_MODULE": "backend.Insight.settings",
    "STATIC_ROOT": "frontend/build/static",
    "STATIC_BUILD_OUT_DIR": "frontend/build"
  },
  "public": true
}
